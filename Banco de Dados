
CREATE DATABASE my_pc_pokemon;
USE my_pc_pokemon;

CREATE TABLE box_pokemon (
    id_box INT AUTO_INCREMENT PRIMARY KEY,
    nome_box VARCHAR(50) NOT NULL    
    )
    
CREATE TABLE pokemons (
    id INT AUTO_INCREMENT PRIMARY KEY,
    id_box INT NOT NULL,
    nome VARCHAR(50) NOT NULL,
    tipo VARCHAR(30) NOT NULL,
    level_pokemon INT NOT NULL CHECK(level_pokemon >= 1 AND level_pokemon <= 100),
    hp INT NOT NULL,
    atk INT NOT NULL,
    def INT NOT NULL,
    sp_atk INT NOT NULL,
    sp_def INT NOT NULL,
    speed INT NOT NULL,
    FOREIGN KEY (id_box) REFERENCES box_pokemon(id_box)
    );



DB_HOST=localhost
DB_USER=root
DB_PASSWORD=senai2025
DB_NAME=estoque
PORT=5500
    
